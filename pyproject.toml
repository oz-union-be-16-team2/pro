[project]
name = "pro"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.128.0",
]

[dependency-groups]
dev = [
    "aerich>=0.9.2",
    "ruff>=0.14.13",
]
prod = [
    "asyncpg>=0.31.0",
    "tortoise-orm>=0.25.3",
]
